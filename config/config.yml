logger:
    log-level: "info"
    log-file: "/var/log/avito.log"

postgres:
    host: "localhost"
    port: 5432
    user: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    db: ${POSTGRES_DB}
    db-timeout-sec: 5